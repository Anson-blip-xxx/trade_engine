# V2 剩余开发与验收清单

2026-09-20 代码核查。本文跟踪独立 V2 新架构，不继承历史路线图的 CLOSED 状态。
**当前不是可上线版本，整项升级尚未完成。** 不得把新增核心模块或模拟测试通过
解释成旧服务已迁移，也不得因此执行平仓、清库、重启或生产切换。

最新[用户授权清理结果](V2_RESET_RESULT_20260921.md)：旧 Redis 487 键及 19 个本地
状态文件已备份并移出运行路径，旧六服务已禁开机自启。ZORA 测试仓只减仓关闭遭
币安 -2022 拒绝，仍未平掉；不能把本地清理报告为交易所账户清零。

2026-09-21 用户新增授权：旧交易服务已停止，开始独立测试网环境实际联调；
Polymarket 排除，TradingView 后置，TG复用现有配置。执行记录与当前阻断见
[测试网接管与真实联调](V2_TESTNET_DEPLOYMENT_20260921.md)。本授权不代表门禁已通过。

用户随后明确授权[旧测试账户清理](V2_TESTNET_CLEANUP_20260921.md)：2个旧仓已平、
100个条件单已逐个确认撤销，最终0/0/0账户基线已落库，移交阻断解除。
这是维护操作，不关闭真实策略/保护单/自动结算的开发与验收门禁。

## 已具备的核心能力

- PG 信号、不可变决策与配置、意图、订单 CAS、成交账本、资金流水及资金费分配。
- UNKNOWN 原身份查询、不重复提交；历史费用估值、精确盈亏、结算版本绑定。
- Redis 可重建投影、ClickHouse 事件去重、outbox 回执及带租约重试。
- 新增 `StrategyScheduler`：PG 持久任务、公平发现、逐条领取、失败退避、
  租约回收与令牌隔离；容量等待继续处理，原决策期限不延长。
- 隔离事务/并发/故障注入、备份恢复、临时 PG 崩溃恢复；新增贯穿测试将
  信号→策略→Binance 协议模拟→丢响应恢复→开平仓账本→资金费→结算→Redis 重建连接验证。

以上是模块能力，不代表下列接线工作完成。

## 未完成的发布门禁（按依赖排序）

| 项目 | 现有证据与缺口 | 完成标准 |
| --- | --- | --- |
| 1. 信号和上下文入口（进行中） | 独立 TV WSGI→PG→策略接线、认证/重放、新鲜上下文及期限贯穿已实现；旧 s0/s2/s3/tv_bridge 生产者及 alert 模板尚未全部迁移 | 认证/输入校验、来源身份与期限、行情新鲜度、幂等重放；PG 不可用时不接收新开仓，不降级文件 |
| 2. 账户级原子风控（进行中） | PG 原子预算、归档参考、版本证据、受控工厂及账户维护隔离已接线；真实保证金与来源在线验收仍缺 | 并发预算/仓位数/冷却预留、配置版本、未知订单保留额度、确认释放、重启重建、账户和环境隔离；可信参考与真实资金风险验收 |
| 3. 策略迁移 | `StrategyWorker` 可固定纯决策；S6/S8 仍调用 `shared_executor.open_position`；S7 网格等未接线 | 迁移真实规则及状态、规则回放/差异测试、所有开单只能先持久登记、禁止旧执行器绕行；不能用固定 qty 的 QA 回调替代真实策略 |
| 4. PM 与保护单 | 有平仓数量预留、Testnet 保护登记及原生子单接管；TP 自然触发已实际通过。旧 PM 尚未迁移为新核心常驻生命周期 | 开仓部分成交即有对应保护；条件单身份/触发/撤换/查询/崩溃恢复；PG 故障不撤已有保护；未知结果不盲重下 |
| 5. 全账户对账与启动恢复 | 原始 income 分页入库及独占协议单三方证据结算已通过；通用多策略/资金费/迟到流水对账未接线 | 真实持仓/普通单/条件单/成交/资金流水共同核验；识别外部操作和强平；全量与增量覆盖；异常阻止新开仓；自动生成结算证据而非手填布尔值 |
| 6. 网格与估值接线 | 子订单数量预算、分批平仓和估值修订已有 | S7 生产计划、撤单与重挂幂等、部分成交重启恢复；历史汇率来源与时间误差校验；缺证据保持 PENDING |
| 7. 常驻运行与最低告警 | 数据侧 pilot、隔离 PG/Redis/CH 已运行，TG 回执已验证；Testnet 统一 daemon 装配和监督循环已实现，真实依赖入口与可重启发布部署未完成 | 显式账户/环境绑定、默认禁交易、有限超时、统一配额、恢复先于开仓、优雅停机、权限收敛、PG/行情/保护异常自动告警 |
| 8. 全系统验收及切换演练 | 有核心模拟贯穿及真实固定协议保护/平仓/结算测试，尚无全部真实策略＋保护＋自动对账的独立环境验收 | 全链路隔离 QA、依赖/系统包锁定、最小权限、备份/回滚演练；完成后单独确认生产切换窗口 |

策略清单须覆盖 s0、s2、s3、s6、s7、s8、s8b、pm、tv_bridge；本轮在
`services/`、`strategies/` 中未找到直接命名为 S8B 的实现，不据此宣称该模块不存在
或已完成迁移，需结合历史交接与运行服务映射定位。此处 pm 指仓位管理，不是 Polymarket。

第 1 项当前契约、部署边界和未完成接线详见 [信号入口说明](V2_SIGNAL_INGRESS.md)。
后续已补充 [生产批次及 Redis 上下文接线](V2_PRODUCER_PUBLICATION.md)，通过隔离贯穿 QA。
已补充 [S3 持久生命周期及纯检测接线](V2_S3_DURABLE_RUNTIME.md)，包括冷却、失败突破、
稳定事件身份和状态/信号/批次原子提交。下一依赖是实际行情采集/窗口输入、来源时间
验证和新常驻循环；S0/S2 与旧入口迁移仍未完成，第 1 项未关闭。

已新增 [已收盘分钟行情窗口及可重试 runner](V2_S3_CANDLE_INPUT.md)：严格完整输入、
来源时间/摘要、纯窗口构建和提交后源确认。实际网络采集、持久源重放/确认和
监督循环仍未实现，不能把注入行情源的 QA 当成生产接入完成。

后续新增 [ClickHouse 原始归档与 PG 源投递账本](V2_S3_SOURCE_RECOVERY.md)，实现
持久身份、顺序租约、摘要绑定确认、过期/损坏隔离和重启重放。网络采集、常驻监督、
积压限流、归档保留及集群恢复验证仍未完成；第 1/7/8 项继续保持开放。

当前又补充 [采集、分块归档与监督工厂](V2_MARKET_PIPELINE.md)：已实现只读 Binance
HTTP 采集、PG 并发权重/退避、待处理上限、小时块复用、同边界跳过和恢复优先循环。
尚未部署或实际网络验收；CH 集群贯穿、保留治理和规模验证仍是门禁。

后续补充 [行情持久告警](V2_OPERATIONAL_ALERTS.md)：独立 PG 运维 outbox、
故障合并、隔离审计补扫、租约重试和回执，监督工厂默认接线。实际 TG 传输、
PG 故障时的外部监控和常驻部署仍未完成，第 7 项仍开放。

账户预算核心契约与不能替代的真实资金风控见 [账户级原子准入预算](V2_ACCOUNT_RISK.md)。
后续已补 [归档价格参考与受控端口工厂](V2_ARCHIVED_RISK_REFERENCE.md)：PG 确认批次、
CH 摘要/重算校验、精确原始 close、期限贯穿、账户绑定和默认禁交易。真实在线验收、
采集权限、保证金/自动对账和真实策略接线仍未完成，第 2/3/7 项开放。
现已补 [账户维护隔离](V2_ACCOUNT_MAINTENANCE.md)：恢复、过期、告警、额度释放与
财务投递均先筛选账户再领取；跨账户直接调用不修改对方订单。权限和部署验收仍待完成。

第 3 项已补 [S6/S8 纯规则与精确仓位计算](V2_DIRECTIONAL_RULES.md)，包括历史表现、
R:R 和资金费率门槛；移除预算不足时抬升最小量的行为。已接 PG 持久化回放，
消费者独立且不创建订单；当前仅生成并保存候选计划，
尚未接通真实策略调度、PG 冷却/回撤/仓位限制或保护单，不能关闭第 3/4/8 项。

2026-09-21 已补 [PG 余额回撤状态及回放上下文](V2_DRAWDOWN_STATE.md)：峰值、
暂停/恢复/亏损锁定、并发去重与历史记录持久化，余额/系数及期限进入决策证据。
真实余额来源、提交事务内校验、策略仓位/冷却及保护单仍开放，不构成实盘许可。

2026-09-21 [历史数据驱动的退出修正](V2_HISTORICAL_EXIT_FINDINGS.md)：V2 既有 PM
修复亏损反转跳过时间止损、延期截止时间失效，以及同轮平仓失败/异常后的重复尝试。
不改旧运行环境；尚未接通新 PG 保护单生命周期，第 3/4/8 项继续开放。

当前已真实验证账户只读清点→新PG→TG回执，以及BTC/ETH公共行情→新CH→S3→PG→Redis。
修复真实CH驱动FixedString摘要bytes兼容；数据侧pilot不是完整交易daemon。
下单、保护、平仓、自动对账及结算的真实测试网闭环仍未完成。

2026-09-21 已按用户授权开始[保护单真实协议探测](V2_PROTECTION_PROTOCOL_20260921.md)：
新增测试网限定 close-all 传输、PG 提交前 CAS 和原身份查询恢复。空仓正向创建尚未通过，
实测包含价格越界拒绝 -4007 与修正价格后的 -4509，最终账户仍 0/0/0；没有策略开仓。
测试网是当前开发与验收环境，不再等待额外环境授权；第 2/3/4/5/8 项仍开放。

继续调试后，[单笔有仓保护协议闭环](V2_PROTECTED_ROUNDTRIP_20260921.md)已真实通过：
PG 预算与订单→BTC 小额开仓→止损/止盈 NEW→reduce-only 平仓→成交/费用落库→0/0/0。
修复平仓自动 EXPIRED 与撤单 -2011 竞争；恢复重跑不新增订单。income 与钱包差额匹配。
这是固定协议单，不是真实策略自动循环；触发成交接管、常驻 PM、自动资金结算仍未关闭。

后续[单笔协议结算](V2_PROTOCOL_SETTLEMENT_20260921.md)已实际 SETTLED：自动核对逐笔 income、
成交、钱包差额，生成证据并释放预算；重跑 ALREADY_SETTLED。固定独占协议案例已落地，
全账户/多策略/资金费/迟到流水重审仍未接线；不能据此关闭第 5/8 项。

## 最新完成节点与仍开放的门禁

新增 [真实账户核验与受控首单入口](V2_VENUE_READINESS.md)：实际账户权限、杠杆/
保证金模式、USDT 余额及敞口核验，证据先落库再调用 submit；绑定 S6/S8 原始计划。
Testnet 只读实测因已有仓位和实际配置不匹配正确阻断，没有新增交易所写请求。
仅空仓首单、默认禁用；多仓资金风险、完整常驻流程及真实发单验收仍开放。

新增 [S6/S8 正式准入与原始止损恢复](V2_DIRECTIONAL_ADMISSION.md)：复用真实规则，
独立正式消费者将合格候选登记为 PG PREPARED，接入持久调度；保护从最初决策取价，
接入开仓冻结/撤余单/受控止损。默认禁用，尚未常驻部署或允许真实自动交易。
真实上下文、保证金/杠杆核验、完整 PM 和通用结算继续开放，不关闭第 3/4/8 项。

用户最新决定：ZORA 暂不处理；保留账外异常与原始证据，不再尝试平仓。
其余开发和隔离 QA 继续，不能把搁置解释成共享测试账户已通过新交易安全门禁。

新增[部分开仓撤余单与残仓保护](V2_PARTIAL_OPEN_RECOVERY.md)：PG 开仓冻结同时作用于
订单准备和提交，持久撤单登记后单次请求，原身份成交核验后才安装残仓止损。
默认禁写、未部署交易权限；仍不能声称实现“部分成交立即无窗口保护”，撤单未确认
及多活跃订单需要后续 PM 调度处置。真实策略、完整 PM、通用对账和发布门禁仍开放。

新增[受控止损创建组件](V2_GUARDED_STOP_INSTALL.md)：默认禁创建，已确认仓位/账户覆盖/
新鲜价格/精确 tick 核验，事务内复核并登记后才允许单次提交，已有登记只查询恢复。
本轮仅隔离 QA，未启用运行服务写权限；部分成交保护、动态撤换及常驻策略接线仍未完成。

2026-09-21 新增[账户持仓与保护覆盖核验](V2_ACCOUNT_COVERAGE_20260921.md)，
对比 PG 前后版本、交易所实际持仓/普通单/条件单与完整止损条款，只读诊断不授权交易。
实际发现账外 `ZORAUSDT +26399`，标记 BLOCKED 并成功 TG 通知；来源未确定，
未自动平仓或接管，必须在后续新增交易验收前解决。全账户资金归因和完整 PM 仍未完成。

2026-09-21 补充[保护单常驻恢复巡检](V2_PROTECTION_SUPERVISOR_20260921.md)：
PG 公平发现、GET-only 原生子单恢复、账户互斥、待恢复告警 outbox/TG、终态退出轮询。
实际只读核对 5 条保护记录后重跑无待处理项，无新增订单。仅覆盖已登记保护，
尚不发现未登记裸仓，不创建/撤换保护，也不是完整 PM 或开仓授权。

2026-09-21 [原生子单真实接管与结算](V2_NATIVE_CHILD_20260921.md)已通过 TAKE_PROFIT
自然触发：父 algo→原生 MARKET 子单→FILLED→同 episode 账本→SETTLED；未另发平仓单。
补充同身份查询可见性重试、子单 query-only 约束、残量退出，以及三方证据支持的
1e-8 USDT 显式精度调整与原子结算。STOP 两次探测未观察到自然触发，未标通过；
真实常驻 PM、退出竞争、策略自动接线与全账户对账仍开放，第 3/4/5/8 项不关闭。

新增 [S6/S8 主动退出与保护单竞争恢复](V2_DIRECTIONAL_EXIT_STAGE.md)：正式开仓证据、
实际成交、PG 峰值/分批状态和新鲜行情共同生成退出决定；默认禁写，显式 Testnet 门禁后
才登记并发送 reduce-only CLOSE。已补 ACK/UNKNOWN 原身份恢复，以及原生止损 child 与
主动退出同时存在时的账本接管，不撤止损制造裸仓窗口。退出观察已接受配额 Binance
公共 mark/funding 及严格已收盘 15m/1h K 线，数量步进取不可变开仓证据。尚未部署
常驻循环、真实自然信号验收或实现动态保本/移动止损替换，第 4/7/8 项继续开放。

新增 [Testnet 统一交易守护进程](V2_TESTNET_DAEMON.md)：实际保护、退出、结算阶段已由
受限 SANDBOX 工厂装入统一流水；开仓、保护写入和只减仓退出拆为独立权限，支持禁止
增加风险时继续保护/退出。每轮先落 PG 启动检查点，监督异常只发送脱敏告警并有界等待。
现已补严格服务配置契约及无需 PG 的脱敏 TG 外层故障适配器；尚缺真实账户上下文、
配置/密钥加载进程入口、systemd 及长时间真实 Testnet 演练，因此第 7/8 项仍开放，
本轮没有向 Binance 发送请求。

## 可延后的增强

TG 富消息与 Web 操作台按用户授权后置；最低故障通知仍是生产门禁。
通知和迟到的人工确认不得直接复活过期信号；重新交易必须基于新场景、新风控和新意图。

## QA 边界

所有新测试仅使用一次性 PG/Redis、独立 ClickHouse local 和注入交易所响应。
贯穿测试确实经过 `BinanceFutures` 协议解析，但没有真实 API、保护单，
风险检查和结算证据是显式 QA 替身，不能称为生产完整 E2E。
盈利/亏损数值证明的是账务拆解，不是策略因果或未来收益承诺。

当前初始化 SQL 是 greenfield schema，不是已部署数据库的升级脚本。
未来切换前必须验证实际 schema/权限/依赖；当前开发不对运行数据库执行它。
